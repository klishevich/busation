<% provide(:title, t(:step2_title)) %>
	<%= render 'shared/navigation2' %>
		<div id="contact" class="fullSection">
			<div class="container">
			
				<div class="section-heading">
				      <% if !params[:type].blank? %>
				      	<% if params[:type] == "consult" %>
				      	  <h2>Записаться на бесплатную skype-консультацию</h2>
				      	<% end %>
				      	<% if params[:type] == "site" %>
				      	  <h2>Заказать сайт</h2>
				      	<% end %>
				      	<% if params[:type] == "seo" %>
				      	  <h2>Заказать продвижение сайта</h2>
				      	<% end %>
				      	<% if params[:type] == "support" %>
				      	  <h2>Заказать поддержку сайта</h2>
				      	<% end %>					      			      	
				      <% else %>
				        <h2><%= t(:step2_title) %></h2>
				      <% end %>
				</div>
				
				<div class="row">
					<!-- contact form -->
					<div class="col-md-6 col-sm-6">
						<div class="contact-form  wow fadeInLeft showdelay2">
							<%= render 'form' %>
						<div class="clearfix"></div>
						</div>
					</div>
					<!-- ./contact form -->
					
				</div>
				<!-- /row -->
									
			</div>		